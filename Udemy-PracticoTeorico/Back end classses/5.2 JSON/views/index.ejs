<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <% if(locals.recipe) { %> <%# Cuando le lleguen los datos vamos a mostrar los ingredientes %>

      <h2><%= recipe.name %></h2>

      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>Protein name:<%= recipe.ingredients.protein.name %> , protein preparation: <%= recipe.ingredients.protein.preparation %></li>
        <li>Salsa name: <%= recipe.ingredients.salsa.name %></li>
        <li>  <h3>toppings:</h3> <% for (var i = 0;i<recipe.ingredients.toppings.length;i++){ %>
                  <h5><%= recipe.ingredients.toppings[i].name %> </h5>
                  <ul id="ingredientsList">
                      <li>quantity: <%= recipe.ingredients.toppings[i].quantity %></li>
                      <li>ingredients: <%= recipe.ingredients.toppings[i].ingredients %></li>
                  </ul>
          <% } %> </li>
      </ul>


      <% } else { %> <%# si no llegan los datos mostramos lo siguiente %>
        <h2>Pick your favourite taco ingredient 👆</h2>
   <% } %>
    
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->


  </div>
</body>

</html>