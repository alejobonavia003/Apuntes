//esta forma es nueva para mi para llamar funcions 

function sumar (num1, num2) {
    return num1 + num2;
}

function multiplicar(num1, num2) {
    return num1 * num2;
}

function calculator(num1, num2, operacion) {
    return operacion(num1, num2);
}

/* 
de esta forma opetacion eria un texto donde se supone que cargamos una llamada
a una funcion para luego usar ese texto para llamar a la funcion elejoda
*/

