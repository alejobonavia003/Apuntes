<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="chicken">🍗</button>
    <button type="submit" value="beef" name="beef">🥩</button>
    <button type="submit" value="fish" name="fish">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
     <% if(locals.ingredientes){ %>

      <h2><%= ingredientes.name %></h2>
        <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><%= ingredientes.ingredients.protein.name %>, <%= ingredientes.ingredients.protein.preparation %></li>
        <li><%= ingredientes.ingredients.salsa.name%></li>
        <% for(var i=0;i<ingredientes.ingredients.toppings.length;i++){ %>
        <li><%= ingredientes.ingredients.toppings[i].name %> : <%= ingredientes.ingredients.toppings[i].quantity %></li>
        <% } %>
      </ul>

      <% } else { %>
      <!-- If no recipe yet, show: -->
      <h2>Pick your favourite taco ingredient 👆</h2>

     <% } %>




  </div>
</body>

</html>